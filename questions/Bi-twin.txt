Retrieve all columns from wf_sales and add :
    - A column moving_total_three_last_rows that calculates the moving total between the current row and the two preceding.
    - A column running_total that calculates the running total.
